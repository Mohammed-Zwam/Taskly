{
    "version": 2,
    "framework": "angular",
    "buildCommand": "npm run build",
    "outputDirectory": "dist/Taskly/browser",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}